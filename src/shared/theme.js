import { YELLOW, BLUE, WHITE, BLACK, ORANGE } from '../constants/color';

const palette = {
  YELLOW,
  BLUE,
  WHITE,
  BLACK,
  ORANGE,
}

const fontSize = {
  large: '1.5rem',
  small: '0.6rem',
}

export const theme = {
  palette,
  fontSize,
}


